bundle:
  name: databricks-test-cicd

include:
  - PFL/resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: /Users/stuti@ganitinc.com/.bundle/${bundle.name}/${bundle.target}

  uat:
    mode: production
    workspace:
      root_path: /Users/stuti@ganitinc.com/.bundle/${bundle.name}/${bundle.target}

  prod:
    mode: production
    workspace:
      root_path: /Users/stuti@ganitinc.com/.bundle/${bundle.name}/${bundle.target}
